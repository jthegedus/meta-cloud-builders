name: build-meta-triggers
description: "Build meta-trigges builder on change"
github:
  owner: jthegedus
  name: meta-cloud-builders
  pullRequest:
    branch: .*
filename: trigger-sync/cloudbuild.yaml
includedFiles:
  - meta-triggers/**
